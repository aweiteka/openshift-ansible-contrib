---
# ensure these imagestreams are patch applied to all slave clusters
# FIXME
imagestreams:
  - name: test
    project: aweiteka
# ensure these serviceaccounts on registry cluster
# have a docker-registry secret on other clusters
# FIXME: this should be a list
dockerpullsecret:
  serviceaccount: aaron
  project: aweiteka
  registryhost: docker-registry.default.svc:5000
